class Basket {

    // properties
    numberOfItems = 1;
    Basket;

    constructor(numberOfItems, Basket) {
        // set values for object instance
        this.numberOfItems = numberOfItems;
        this.Basket = Basket;
    }

    // messages
    //numberOfItems() { }
}